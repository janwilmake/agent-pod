{
  "name": "@oauth-solid-broker/oauth2-mock-server",
  "version": "1.0.0",
  "description": "A clean TypeScript OAuth2 mock server for development and testing",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "oauth2-mock-server": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["oauth2", "mock", "server", "testing", "development"],
  "license": "MIT",
  "dependencies": {
    "oauth2-mock-server": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}