{
  "name": "solid-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8789",
    "dev:logged": "mkdir -p ../e2e/logs && wrangler dev --port 8789 2>&1 | tee ../e2e/logs/solid-broker.log",
    "start": "wrangler dev --port 8789",
    "deploy": "wrangler deploy --minify",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "@cloudflare/vitest-pool-workers": "^0.9.1",
    "typescript": "^5.5.4",
    "vitest": "~3.2.0",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "hono": "^4.6.9"
  }
}

